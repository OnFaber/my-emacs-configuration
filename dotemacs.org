#+TITLE: My Emacs Configuration
#+STARTUP: overview
#+PROPERTY: header-args:emacs-lisp :tangle yes :file ./dotemacs.el

* Package initialization

* Built-in Packages
** Keybindings - Keymap.el

+ Path: /usr/local/share/emacs/31.0.50/lisp/keymap.el.gz

This library deals with the "new" keymap binding interface: The
only key syntax allowed by these functions is the `kbd' one.

#+begin_src emacs-lisp
#+end_src

** Eshell
:PROPERTIES:
:Manual: (info-display-manual "Eshell")
:Resources: [[https://www.emacswiki.org/emacs/EshellPrompt][emacs wiki]] 
:END:
  
Eshell is a shell-like command interpreter implemented in Emacs Lisp.
It invokes no external processes except for those requested by the user.
It is intended to be an alternative to the IELM  REPL(1) for Emacs and with an interface similar to
command shells such as ‘bash’, ‘zsh’, ‘rc’, or ‘4dos’.
  
* Thirdy-part Packages
** 
